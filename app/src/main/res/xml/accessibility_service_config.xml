<?xml version="1.0" encoding="utf-8"?>
<!--
  Content changes are needed as well as window changes: entering an app fires only
  the latter, and at that moment the screen is usually still a loading skeleton.
  notificationTimeout keeps the rate down, since an overloaded accessibility
  service gets disabled by the system.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="1000"
    android:description="@string/accessibility_service_description" />
